# dependencies
/node_modules

# build output
/dist
/build/*
# ...but the app icon and its vector source are checked-in inputs, not output:
# package.json and electron/main.js both reference them by path.
!/build/icon.icns
!/build/icons

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
/logs/

# local env files (contain Plaid secrets — never commit)
.env
.env*.local

# data (user data, not committed)
data/
